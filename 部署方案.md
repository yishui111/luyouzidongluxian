# 部署方案（E:\zidongluxian）

> 本方案用于在新电脑上复原本项目；**每次优化/修复后必须同步更新本文件**。

## 1. 项目简介

新疆旅游路线自动生成器：纯静态网页工具。输入景点名称（中文/拼音）自动在地图上连成
旅游轨迹，内置北疆经典、独库公路、南疆风情、全景大环线示例，支持轨迹颜色自定义。

## 2. 新电脑要求

- 任意系统 + 现代浏览器即可；无依赖、无后端、无需安装

## 3. 目录结构

- `index.html`：入口页面
- `css\`、`js\`：样式与逻辑
- `data\`：景点/地图数据
- `预览图.png`：效果预览

## 4. 启动方法

- 直接双击 `index.html` 用浏览器打开；或起本地静态服务：
  在项目目录运行 `python -m http.server 8077`，浏览器访问 http://localhost:8077

## 5. 验证

- 页面打开后输入景点（如"喀纳斯"）能出轨迹；示例按钮可切换线路；颜色选择生效

## 6. 更新约定

每次优化/修复后同步更新本文件。
