# 项目速览：新疆旅游路线生成器（luyouzidongluxian）

> 给 AI 助手看的项目备忘。需要细节时再读 `部署方案.md`，不要全盘扫描。

## 这是什么

新疆旅游路线自动生成器：**纯静态网页工具**。输入景点名称（中文/拼音）自动在地图上连成旅游轨迹。
内置北疆经典、独库公路、南疆风情、全景大环线示例，支持轨迹颜色自定义。

## 技术形态

- 无依赖、无后端、无构建：`index.html` + `css\` + `js\` + `data\`
- 任意系统 + 现代浏览器即可

## 目录结构

```
index.html            # 入口页面
css\style.css         # 样式
js\main.js            # 交互逻辑
data\attractions.js   # 景点数据
data\xinjiang.js      # 新疆地图数据
预览图.png            # 效果预览
部署方案.md            # 部署说明
```

## 启动

- 直接双击 `index.html`；或 `python -m http.server 8000` 后访问 http://localhost:8000

## 状态

- 已完成的小工具，无配套项目，独立运行
